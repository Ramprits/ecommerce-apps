import {createTheme} from '@material-ui/core/styles';

const theme = createTheme({
    "palette": {
        "secondary": {
            "main": "#9c27b0",
            "light": "#ba68c8",
            "dark": "#7b1fa2"
        }
    },
    "shape": {
        "borderRadius": 12
    }
});

export default theme;

